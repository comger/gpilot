# G-Pilot 系统架构设计文档 V2.0

> **文档版本**：2.0（在 V1.0 基础上重大升级）
> **创建时间**：2026-02-23
> **架构师**：Antigravity (AI Architect)
>
> **V2.0 核心变更**：
> 1. 📡 **多模态 AI 层**：将付费 GPT-4o 替换为以免费为主的多模态 API 优先策略
> 2. 🧠 **知识库层**：录制记录不再是"临时数据"，升级为基于本体论的持久化知识图谱
> 3. 🏗️ **业务复刻层**：借助本体论从录制数据中自动推断并轻量复刻目标业务系统结构

---

## 一、架构设计审查意见

### 1.1 V1.0 存在的问题

| # | 问题域 | 具体问题 | V2.0 解决方案 |
|---|---|---|---|
| 1 | **AI 成本** | 默认使用 GPT-4o-mini，每次生成文档均产生费用，政务项目预算敏感 | 引入免费 VLM 优先策略（Gemini Free / Qwen-VL / 本地 Ollama） |
| 2 | **数据价值** | 录制数据仅用于一次性文档生成，用完即删，价值浪费 | 将步骤录制升级为"本体知识入库"，形成可复用知识图谱 |
| 3 | **扩展性** | 文档生成是终态，无法支撑后续 AI 测试、自动化回归等场景 | 通过本体论设计业务系统影子模型，为 RPA / 测试 / 培训奠定基础 |
| 4 | **跨项目复用** | 每个项目独立录制，相似操作模式无法共享 | 知识图谱跨项目共享，支持模式推荐和相似流程检索 |

---

## 二、V2.0 完整系统架构图

```
┌────────────────────────────────────────────────────────────────────────────────────────┐
│                              Chrome Browser Environment                                  │
│                                                                                         │
│  ┌──────────────────────────────────────────────┐  ┌────────────────────────────────┐  │
│  │         Target Web Page (Inspected Tab)       │  │    Extension Popup (React UI)  │  │
│  │                                               │  │                                │  │
│  │  ┌──────────────────────────────────────────┐ │  │ ┌──────────┐  ┌─────────────┐ │  │
│  │  │            Content Script Layer           │ │  │ │ Project  │  │  Knowledge  │ │  │
│  │  │                                           │ │  │ │  Mgmt   │  │  Explorer   │ │  │
│  │  │  ┌────────────────┐  ┌─────────────────┐ │ │  │ └──────────┘  └─────────────┘ │  │
│  │  │  │ Event Listener │  │ Semantic Parser  │ │ │  │ ┌──────────┐  ┌─────────────┐ │  │
│  │  │  │ click/input/   │  │ XPath/ARIA/Role  │ │ │  │ │Recording │  │  Business   │ │  │
│  │  │  │ select/drag    │  │ DOM Fingerprint  │ │ │  │ │  Control │  │  Ontology   │ │  │
│  │  │  └────────────────┘  └─────────────────┘ │ │  │ └──────────┘  │  Viewer     │ │  │
│  │  │                                           │ │  │ ┌──────────┐  └─────────────┘ │  │
│  │  │  ┌───────────────────────────────────────┐│ │  │ │ Masking  │  ┌─────────────┐ │  │
│  │  │  │      Privacy Masking Engine (Local)   ││ │  │ │  Config  │  │  Doc Preview│ │  │
│  │  │  │  Regex Matcher │ Manual Click Marker  ││ │  │ └──────────┘  │  & Export   │ │  │
│  │  │  │  Canvas Overlay Renderer              ││ │  │               └─────────────┘ │  │
│  │  │  └───────────────────────────────────────┘│ │  └────────────────────────────────┘  │
│  │  │                                           │ │                                       │
│  │  │  ┌───────────────────────────────────────┐│ │  ┌────────────────────────────────┐  │
│  │  │  │   Screenshot Capture Module            ││ │  │    Floating Console (Sidebar)  │  │
│  │  │  └───────────────────────────────────────┘│ │  │  Mark/Alias │ Step Indicator   │  │
│  │  └──────────────────────────────────────────┘ │  └────────────────────────────────┘  │
│  └──────────────────────────────────────────────┘                                        │
│                                                                                         │
│  ┌──────────────────────────────────────────────────────────────────────────────────────┐│
│  │                           Service Worker (Background)                                  ││
│  │                                                                                       ││
│  │  ┌──────────────────┐  ┌──────────────────────┐  ┌───────────────────────────────┐  ││
│  │  │  Session Manager │  │   AI Orchestrator    │  │      Knowledge Manager        │  ││
│  │  │  录制生命周期    │  │  ┌────────────────┐  │  │  ┌─────────────────────────┐  │  ││
│  │  │  状态机管理      │  │  │ VLM Router     │  │  │  │  Ontology Engine        │  │  ││
│  │  │  Tab 追踪        │  │  │ (免费优先策略)  │  │  │  │  (本体推理引擎)          │  │  ││
│  │  └──────────────────┘  │  └────────────────┘  │  │  └─────────────────────────┘  │  ││
│  │                        │  ┌────────────────┐  │  │  ┌─────────────────────────┐  │  ││
│  │  ┌──────────────────┐  │  │ Prompt Builder │  │  │  │  Knowledge Graph CRUD   │  │  ││
│  │  │  Doc Generator   │  │  └────────────────┘  │  │  └─────────────────────────┘  │  ││
│  │  │  模板渲染         │  │  ┌────────────────┐  │  │  ┌─────────────────────────┐  │  ││
│  │  │  双视图生成       │  │  │ Fallback Chain │  │  │  │  Pattern Recognizer     │  │  ││
│  │  └──────────────────┘  │  └────────────────┘  │  │  │  (流程模式识别)          │  │  ││
│  │                        └──────────────────────┘  │  └─────────────────────────┘  │  ││
│  │  ┌──────────────────┐  ┌──────────────────────┐  └───────────────────────────────┘  ││
│  │  │  Export Engine   │  │   Storage Manager    │                                      ││
│  │  │  docx/pdf/md     │  │   IndexedDB CRUD     │  ┌───────────────────────────────┐  ││
│  │  └──────────────────┘  └──────────────────────┘  │   Business System Replicator  │  ││
│  │                                                   │  ┌─────────────────────────┐  │  ││
│  │  ┌──────────────────┐  ┌──────────────────────┐  │  │  System Schema Inferrer  │  │  ││
│  │  │  Config Manager  │  │   Masking Manager    │  │  └─────────────────────────┘  │  ││
│  │  │  API Keys / 规则  │  │   规则库持久化        │  │  ┌─────────────────────────┐  │  ││
│  │  └──────────────────┘  └──────────────────────┘  │  │  Workflow Simulator      │  │  ││
│  └──────────────────────────────────────────────────┘  │  └─────────────────────────┘  │  ││
│                                                          └───────────────────────────────┘  ││
│  ┌──────────────────────────────────────────────────────────────────────────────────────┐ ││
│  │                      IndexedDB (Layered Local Storage)                                │ ││
│  │                                                                                       │ ││
│  │  [Layer 1: Raw Data]  projects │ sessions │ steps │ screenshots │ masking_rules       │ ││
│  │  [Layer 2: Knowledge] ontology_nodes │ ontology_edges │ ui_concepts │ biz_workflows   │ ││
│  │  [Layer 3: System]    system_schema │ interaction_patterns │ field_mappings           │ ││
│  └──────────────────────────────────────────────────────────────────────────────────────┘ ││
│                                                                                            ││
└────────────────────────────────────────────────────────────────────────────────────────────┘│
                          │                                    │
              ┌───────────▼──────────────┐      ┌─────────────▼─────────────┐
              │  Free-First VLM API Layer │      │   Optional Paid API Layer  │
              │                          │      │   (用户自配 API Key 才启用) │
              │  ┌─────────────────────┐ │      │  ┌───────────────────────┐ │
              │  │ Gemini 2.0 Flash    │ │      │  │  OpenAI GPT-4o-mini   │ │
              │  │ (Free Tier 首选)    │ │      │  │  Anthropic Claude     │ │
              │  └─────────────────────┘ │      │  └───────────────────────┘ │
              │  ┌─────────────────────┐ │      └───────────────────────────┘
              │  │ Qwen2.5-VL (免费版) │ │
              │  │ via OpenRouter Free │ │      ┌────────────────────────────┐
              │  └─────────────────────┘ │      │   Local LLM (完全免费)     │
              │  ┌─────────────────────┐ │      │  ┌──────────────────────┐  │
              │  │ GLM-4V Flash (免费)  │ │      │  │  Ollama + Qwen2-VL   │  │
              │  │ via Zhipu API       │ │      │  │  Ollama + LLaVA      │  │
              │  └─────────────────────┘ │      │  │  (零成本，零隐私风险)  │  │
              └──────────────────────────┘      │  └──────────────────────┘  │
                                                └────────────────────────────┘
```

---

## 三、模块职责说明（V2.0 更新）

| 模块 | 所在位置 | 核心职责 | V2.0 变更 |
|---|---|---|---|
| **Event Listener** | Content Script | 监听 Click/Input/Select/Drag 事件 | 无变化 |
| **Semantic Parser** | Content Script | 提取 XPath/ARIA/DOM Fingerprint | 新增 DOM 指纹生成，用于跨录制匹配 |
| **Privacy Masking Engine** | Content Script | 正则 + 点选 DOM 替换 | 无变化 |
| **Canvas Overlay Renderer** | Content Script | 截图前遮罩处理 | 无变化 |
| **Session Manager** | Service Worker | 录制生命周期状态机 | 无变化 |
| **AI Orchestrator** | Service Worker | LLM 调用调度 | **重构**：引入 VLM Router 免费优先链 |
| **Knowledge Manager** | Service Worker | **新模块**：本体知识图谱管理 | 全新模块 |
| **Ontology Engine** | Service Worker | **新模块**：本体推理、概念抽象 | 全新模块 |
| **Business System Replicator** | Service Worker | **新模块**：从知识图谱推断系统架构 | 全新模块 |
| **Doc Generator** | Service Worker | 文档生成 | 增强：可从知识库拉取相似步骤描述 |
| **Export Engine** | Service Worker | docx/pdf/md 导出 | 无变化 |
| **Storage Manager** | Service Worker | IndexedDB 三层存储管理 | **升级**：新增知识层和系统层存储 |

---

## 四、免费多模态 API 优先策略

### 4.1 VLM 提供商对比矩阵

| 提供商 | 模型 | 免费额度 | 并发限制 | 图像质量 | 中文支持 | 隐私风险 |
|---|---|---|---|---|---|---|
| **Google Gemini** | gemini-2.0-flash | 1500 RPD / 15 RPM | 中 | ⭐⭐⭐⭐⭐ | 优秀 | 低（政府可接受） |
| **Zhipu AI** | glm-4v-flash | 100万 Token/天（免费） | 高 | ⭐⭐⭐⭐ | 卓越 | 极低（国内服务） |
| **OpenRouter** | qwen/qwen2.5-vl-72b | 免费（速率有限） | 低 | ⭐⭐⭐⭐⭐ | 优秀 | 中 |
| **Ollama (本地)** | qwen2.5-vl / llava | 完全免费 | 取决于硬件 | ⭐⭐⭐⭐ | 优秀 | 零（完全本地） |
| **OpenAI** | gpt-4o-mini | 无免费额度 | 高 | ⭐⭐⭐⭐⭐ | 优秀 | 中（需用户授权） |

### 4.2 VLM Router — 免费优先路由策略

```typescript
// VLM 调用的优先级链（Waterfall Fallback Pattern）
const VLM_PROVIDER_CHAIN: VLMProviderConfig[] = [
  {
    id: 'ollama-local',
    name: '本地 Ollama (完全免费)',
    priority: 0,                          // 最优先：零成本、零隐私风险
    isAvailable: () => checkOllamaHealth(),
    model: 'qwen2.5-vl:7b',
    baseUrl: 'http://localhost:11434/api',
    costPerImage: 0,
  },
  {
    id: 'zhipu-glm4v-free',
    name: '智谱 GLM-4V-Flash (免费)',
    priority: 1,                          // 次优先：国内服务，中文最佳
    isAvailable: () => hasApiKey('zhipu') && checkDailyQuota('zhipu'),
    model: 'glm-4v-flash',
    baseUrl: 'https://open.bigmodel.cn/api/paas/v4',
    costPerImage: 0,
    dailyLimit: 1_000_000,               // Token 限制
  },
  {
    id: 'gemini-flash-free',
    name: 'Google Gemini 2.0 Flash (免费层)',
    priority: 2,
    isAvailable: () => hasApiKey('gemini') && checkRateLimit('gemini'),
    model: 'gemini-2.0-flash',
    baseUrl: 'https://generativelanguage.googleapis.com/v1beta',
    costPerImage: 0,
    rpmLimit: 15,
    rpdLimit: 1500,
  },
  {
    id: 'openrouter-qwen-free',
    name: 'OpenRouter Qwen2.5-VL (免费)',
    priority: 3,
    isAvailable: () => hasApiKey('openrouter') && checkRateLimit('openrouter'),
    model: 'qwen/qwen2.5-vl-72b-instruct:free',
    baseUrl: 'https://openrouter.ai/api/v1',
    costPerImage: 0,
  },
  {
    id: 'openai-paid',
    name: 'OpenAI (付费，用户自配)',
    priority: 99,                         // 最低优先级，仅在用户明确配置时使用
    isAvailable: () => hasApiKey('openai') && isUserApprovedPaid(),
    model: 'gpt-4o-mini',
    baseUrl: 'https://api.openai.com/v1',
    costPerImage: 0.00015,
  },
];

// 路由执行逻辑
async function routeVLMRequest(request: VLMRequest): Promise<VLMResponse> {
  const chain = VLM_PROVIDER_CHAIN.sort((a, b) => a.priority - b.priority);

  for (const provider of chain) {
    if (await provider.isAvailable()) {
      try {
        const result = await callVLMProvider(provider, request);
        logProviderUsage(provider.id, 'success');
        return result;
      } catch (err) {
        logProviderUsage(provider.id, 'failed', err);
        continue; // 降级到下一个
      }
    }
  }

  // 最终降级：纯文本模式（不调用任何 VLM）
  return generateTextOnlyDescription(request);
}
```

### 4.3 各提供商 API 适配层

```typescript
// 统一 VLM 接口，屏蔽各厂商差异
interface VLMAdapterFactory {
  create(providerId: string): VLMAdapter;
}

interface VLMAdapter {
  generateStepDescription(step: RecordingStep, screenshotDataUrl: string): Promise<string>;
  isHealthy(): Promise<boolean>;
}

// Gemini 适配器示例（REST API，无 SDK 依赖）
class GeminiAdapter implements VLMAdapter {
  async generateStepDescription(step: RecordingStep, img: string): Promise<string> {
    const response = await fetch(
      `${this.baseUrl}/models/gemini-2.0-flash:generateContent?key=${this.apiKey}`,
      {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          contents: [{
            parts: [
              { text: this.buildPrompt(step) },
              { inline_data: { mime_type: 'image/png', data: img.replace(/^data:image\/png;base64,/, '') } }
            ]
          }],
          generationConfig: { maxOutputTokens: 256, temperature: 0.2 }
        })
      }
    );
    const data = await response.json();
    return data.candidates[0].content.parts[0].text;
  }
}

// 智谱 GLM-4V 适配器（兼容 OpenAI 接口规范）
class ZhipuAdapter implements VLMAdapter {
  async generateStepDescription(step: RecordingStep, img: string): Promise<string> {
    // 与 OpenAI 接口完全兼容，直接复用
    return callOpenAICompatibleAPI(this.baseUrl, 'glm-4v-flash', step, img);
  }
}

// Ollama 本地适配器
class OllamaAdapter implements VLMAdapter {
  async generateStepDescription(step: RecordingStep, img: string): Promise<string> {
    const response = await fetch('http://localhost:11434/api/generate', {
      method: 'POST',
      body: JSON.stringify({
        model: 'qwen2.5-vl:7b',
        prompt: this.buildPrompt(step),
        images: [img.replace(/^data:image\/png;base64,/, '')],
        stream: false
      })
    });
    const data = await response.json();
    return data.response;
  }

  async isHealthy(): Promise<boolean> {
    try {
      await fetch('http://localhost:11434/api/tags', { signal: AbortSignal.timeout(2000) });
      return true;
    } catch { return false; }
  }
}
```

---

## 五、本体论知识库设计

### 5.1 设计理念

将每次录制不再视为"一次性文档素材"，而是**向政务业务系统知识库的一次增量提交**。

知识库采用轻量级本体论（Ontology）设计，核心四元组：**概念（Class）→ 属性（Property）→ 实例（Individual）→ 关系（Relation）**

```
知识图谱结构：

  [界面概念层]         [业务语义层]          [系统结构层]
  ┌──────────┐        ┌──────────┐          ┌──────────┐
  │ UIPage   │───────>│ BizPage  │─────────>│ Module   │
  │ UIComp   │        │ BizFlow  │          │ Entity   │
  │ UIAction │        │ BizRole  │          │ Schema   │
  └──────────┘        └──────────┘          └──────────┘
       │                   │                     │
       └───────────────────┼─────────────────────┘
                           │
                    OntologyNode (图谱节点)
                    OntologyEdge (图谱边)
```

### 5.2 本体论数据结构

```typescript
// ─────────────────────────────────────────────────────
// 知识图谱核心结构
// ─────────────────────────────────────────────────────

// 本体节点（所有知识图谱元素的基类）
interface OntologyNode {
  id: string;                             // UUID
  type: OntologyNodeType;
  label: string;                          // 人类可读标签
  properties: Record<string, unknown>;    // 动态属性
  confidence: number;                     // 置信度 0-1（AI 推断时降低，人工确认后为 1）
  createdAt: number;
  updatedAt: number;
  sourceSessionIds: string[];             // 从哪些录制会话中提炼
  humanVerified: boolean;                 // 是否人工核实
}

type OntologyNodeType =
  // 界面概念层
  | 'UIPage'           // 一个独立页面（如"用户登录页"）
  | 'UIComponent'      // 页面上的 UI 组件（如"登录按钮"）
  | 'UIField'          // 输入字段（如"用户名输入框"）
  | 'UIMenu'           // 菜单项
  | 'UIDialog'         // 弹窗 / Modal
  // 业务语义层
  | 'BizWorkflow'      // 完整业务流程（如"新增市民申请"）
  | 'BizTask'          // 原子业务任务（如"填写申请人信息"）
  | 'BizRole'          // 操作角色（如"审批员"、"录入员"）
  | 'BizCondition'     // 业务判断条件（如"如果状态为待审批"）
  | 'BizDataEntity'    // 业务数据实体（如"申请单"、"审批意见"）
  // 系统结构层
  | 'SysModule'        // 系统功能模块（如"申请管理模块"）
  | 'SysField'         // 数据字段（如 field: applicant_name: string）
  | 'SysAPI'           // 推断出的后端 API（如 POST /api/apply）
  | 'SysPermission';   // 权限点

// 知识图谱边（关系）
interface OntologyEdge {
  id: string;
  type: OntologyEdgeType;
  fromNodeId: string;
  toNodeId: string;
  weight: number;                        // 关系强度 0-1
  properties?: Record<string, unknown>;
}

type OntologyEdgeType =
  | 'CONTAINS'          // 包含关系（Page CONTAINS Component）
  | 'TRIGGERS'          // 触发关系（Click TRIGGERS Navigation）
  | 'NAVIGATES_TO'      // 页面跳转（Page NAVIGATES_TO Page）
  | 'PART_OF'           // 构成关系（Task PART_OF Workflow）
  | 'REQUIRES'          // 依赖关系（Task REQUIRES BizCondition）
  | 'MAPS_TO'           // UI-业务映射（UIField MAPS_TO SysField）
  | 'PERFORMED_BY'      // 操作者（Task PERFORMED_BY Role）
  | 'PRECEDES'          // 时序先后（Task.A PRECEDES Task.B）
  | 'SIMILAR_TO';       // 语义相似（Component A SIMILAR_TO Component B）

// ─────────────────────────────────────────────────────
// 界面概念层：UI 概念
// ─────────────────────────────────────────────────────
interface UIPageConcept extends OntologyNode {
  type: 'UIPage';
  properties: {
    urlPattern: string;           // URL 模式（如 /apply/:id/fill）
    pageTitle: string;
    sysModule?: string;           // 所属系统模块（推断）
    accessRoles?: string[];       // 可访问角色（推断）
    visitCount: number;           // 在录制中出现次数
  };
}

interface UIComponentConcept extends OntologyNode {
  type: 'UIComponent';
  properties: {
    componentType: 'button' | 'input' | 'select' | 'table' | 'form' | 'tab' | 'menu';
    domFingerprint: string;       // 元素稳定指纹，用于跨录制匹配
    cssSelector: string;          // 最稳定 CSS 选择器
    xPath: string;
    labelText: string;            // 按钮文字、输入框 label
    ariaRole?: string;
    pageId: string;               // 所属页面节点 ID
    actionCount: number;          // 被操作次数（热度）
  };
}

// ─────────────────────────────────────────────────────
// 业务语义层：业务流程
// ─────────────────────────────────────────────────────
interface BizWorkflow extends OntologyNode {
  type: 'BizWorkflow';
  properties: {
    name: string;                 // 如"市民申请办理流程"
    description: string;          // AI 生成的流程描述
    startPageId: string;          // 流程起始页面
    endPageId: string;            // 流程结束页面
    estimatedDuration: number;    // 预估操作时长（秒）
    taskIds: string[];            // 有序任务列表（BizTask IDs）
    roleIds: string[];            // 涉及角色
    triggerCount: number;         // 该流程被录制次数
  };
}

interface BizTask extends OntologyNode {
  type: 'BizTask';
  properties: {
    name: string;                 // 如"填写申请人基本信息"
    pageId: string;               // 发生页面
    actionSequence: ActionSummary[]; // 操作序列摘要
    inputFields: string[];        // 关联输入字段 IDs
    validationRules?: string[];   // 推断出的校验规则
    isOptional: boolean;          // 是否为可选步骤
  };
}

interface ActionSummary {
  action: ActionType;
  componentId: string;           // UIComponent 节点 ID
  description: string;           // 语义描述
}

// ─────────────────────────────────────────────────────
// 系统结构层：业务系统影子模型
// ─────────────────────────────────────────────────────

// 从录制推断出的系统 Schema
interface SystemSchema {
  id: string;
  projectId: string;
  name: string;                  // 系统名称（如"XX政务服务平台"）
  version: string;               // schema 版本
  inferredAt: number;
  confidence: number;

  modules: SystemModule[];
  entities: DataEntity[];
  workflows: BizWorkflow[];
}

interface SystemModule {
  id: string;
  name: string;                  // 模块名，如"申请管理"
  urlPrefix: string;             // URL 前缀，如 /apply
  pageIds: string[];             // 包含的页面节点 IDs
  parentModuleId?: string;       // 支持模块层级
  permissions: string[];         // 推断需要的权限
}

// 从UI字段推断的数据实体
interface DataEntity {
  id: string;
  name: string;                  // 实体名，如"申请单"
  inferredFromPageIds: string[]; // 从哪些页面推断
  fields: EntityField[];
}

interface EntityField {
  id: string;
  name: string;                  // 字段名（中文），如"申请人姓名"
  technicalName?: string;        // 推断技术名，如 applicantName
  dataType: 'string' | 'number' | 'boolean' | 'date' | 'enum' | 'file';
  isRequired: boolean;
  enumValues?: string[];         // 下拉选项值（从 select 元素录制中提取）
  uiComponentId: string;         // 对应 UIComponent 节点 ID
  validationPattern?: string;    // 推断的校验规则
}
```

### 5.3 知识提炼管线

```typescript
// 录制步骤 → 知识图谱节点的提炼流程
class OntologyEngine {

  // 步骤完成后，将 RecordingStep 提炼为本体节点
  async extractFromStep(step: RecordingStep, session: Session): Promise<KnowledgeExtractionResult> {
    const results: KnowledgeExtractionResult = { nodes: [], edges: [] };

    // 1. 提炼或更新 UIPage 节点
    const pageNode = await this.upsertUIPage(step.pageUrl, step.pageTitle, session.projectId);
    results.nodes.push(pageNode);

    // 2. 提炼或更新 UIComponent 节点（基于 DOM 指纹去重）
    const componentNode = await this.upsertUIComponent(step, pageNode.id);
    results.nodes.push(componentNode);
    results.edges.push({ type: 'CONTAINS', from: pageNode.id, to: componentNode.id });

    // 3. 如果是 navigation 动作，建立页面跳转关系
    if (step.action === 'navigation') {
      const targetPageNode = await this.upsertUIPage(step.inputValue!, '', session.projectId);
      results.edges.push({ type: 'NAVIGATES_TO', from: pageNode.id, to: targetPageNode.id });
    }

    // 4. 如果是 input 动作，提炼 UIField 节点
    if (step.action === 'input') {
      const fieldNode = await this.upsertUIField(step, componentNode.id);
      results.nodes.push(fieldNode);
    }

    return results;
  }

  // 会话结束后，进行高阶业务语义提炼（调用 LLM 辅助）
  async extractWorkflowFromSession(session: Session): Promise<BizWorkflow> {
    const steps = await this.storage.loadSteps(session.id);
    const pageNodes = await this.getPageNodesForSession(session.id);

    // 调用轻量 LLM（文本模型，不需要视觉）进行业务语义分析
    const workflowSummary = await this.llm.analyzeWorkflow({
      steps: steps.map(s => ({
        action: s.action,
        element: s.targetElement,
        maskedText: s.maskedText,
        pageTitle: s.pageTitle,
        url: s.pageUrl,
        aiDescription: s.aiDescription,
      })),
      prompt: WORKFLOW_EXTRACTION_PROMPT
    });

    return this.buildBizWorkflow(workflowSummary, session, pageNodes);
  }

  // DOM 指纹生成（用于跨录制匹配同一元素）
  generateDOMFingerprint(step: RecordingStep): string {
    // 使用 tagName + ariaLabel + role + 相对位置 生成稳定指纹
    // 不依赖 id/class（易变），优先依赖语义属性
    const parts = [
      step.action,
      step.ariaLabel ?? '',
      step.targetElement.split('(')[0].trim(),  // 取语义部分
      extractTagFromSelector(step.targetSelector),
    ];
    return hashFn(parts.join('|'));
  }
}
```

---

## 六、轻量级业务系统复刻层

### 6.1 设计目标

通过跨会话、跨项目的知识图谱积累，自动推断出目标政务系统的**轻量影子模型**，为以下扩展场景奠定基础：

```
V1.0 录制数据  ─→  V2.0 知识图谱  ─→  V3.0+ 扩展能力
                         │
               ┌─────────┼─────────┐
               ▼         ▼         ▼
          AI 测试用例  自动化培训  RPA 脚本
          生成（E2E）  场景生成    辅助生成
```

### 6.2 系统 Schema 推断时序

```
┌──────┐  ┌──────────────┐  ┌───────────────────┐  ┌──────────┐  ┌───────────┐
│  PM  │  │ Knowledge    │  │  Ontology Engine  │  │ LLM API  │  │IndexedDB  │
│(User)│  │  Manager     │  │  (System Inferrer)│  │(文本模型) │  │(知识图谱)  │
└──┬───┘  └──────┬───────┘  └─────────┬─────────┘  └────┬─────┘  └─────┬─────┘
   │              │                   │                  │               │
   │ 多次录制后    │                   │                  │               │
   │ 点击"推断系统架构"               │                  │               │
   │─────────────>│                   │                  │               │
   │              │ loadGraphByProject │                  │               │
   │              │──────────────────────────────────────────────────────>│
   │              │ <────── UIPage[] ──────────────────────────────────── │
   │              │ <────── UIComponent[] ─────────────────────────────── │
   │              │ <────── edges[] ───────────────────────────────────── │
   │              │                   │                  │               │
   │              │ inferSystemSchema()                  │               │
   │              │──────────────────>│                  │               │
   │              │                   │ 图分析:           │               │
   │              │                   │ - URL 聚类 → 模块  │               │
   │              │                   │ - Field 提炼 → 实体│               │
   │              │                   │ - Navigation → 流程│               │
   │              │                   │                  │               │
   │              │                   │ 调用 LLM 语义增强  │               │
   │              │                   │ (纯文本，非 VLM)  │               │
   │              │                   │──────────────────>│               │
   │              │                   │ <── 模块名/实体名 ─│               │
   │              │                   │                  │               │
   │              │                   │ 生成 SystemSchema  │               │
   │              │                   │──────────────────────────────────>│
   │              │ <── schema ──────── ──────────────────────────────── │
   │              │                   │                  │               │
   │ 展示系统架构图│                   │                  │               │
   │<─────────────│                   │                  │               │
```

### 6.3 系统复刻数据结构（扩展接口）

```typescript
// 系统复刻的对外扩展接口
// 未来 V3+ 可基于此接口接入 RPA、E2E 测试、AI 培训等能力

interface BusinessSystemReplica {
  schemaId: string;
  exportFormats: {
    // 导出为 OpenAPI 格式（供测试使用）
    toOpenAPISpec(): OpenAPISpec;
    // 导出为 Playwright 测试骨架
    toPlaywrightTestSkeleton(): string;
    // 导出为 ERD 实体关系描述
    toERD(): ERDSchema;
    // 导出为培训场景脚本
    toTrainingScenario(): TrainingScenario[];
  };
}

// 未来可扩展的知识库查询接口
interface KnowledgeQueryService {
  // 查询相似操作步骤（用于描述推荐）
  findSimilarSteps(step: Partial<RecordingStep>): Promise<RecordingStep[]>;
  // 查询某页面的已知交互模式
  getPageInteractionPatterns(urlPattern: string): Promise<InteractionPattern[]>;
  // 跨项目搜索相似工作流
  searchSimilarWorkflows(sessionId: string): Promise<BizWorkflow[]>;
  // 获取某系统模块的完整知识
  getModuleKnowledge(moduleId: string): Promise<ModuleKnowledge>;
}
```

---

## 七、V2.0 更新后的数据结构定义

### 7.1 原有核心数据模型（保留）

> V1.0 中定义的 `Project`、`Session`、`RecordingStep`、`Screenshot`、`MaskingProfile`、`GeneratedDocument` 结构保持不变，仅在 `RecordingStep` 中增加一个字段：

```typescript
interface RecordingStep {
  // ... 原有字段 ...

  // V2.0 新增：关联知识图谱节点
  ontologyNodeIds?: string[];    // 该步骤提炼出的 OntologyNode ID 列表
  domFingerprint?: string;       // DOM 指纹（用于知识图谱跨录制去重）
}
```

### 7.2 IndexedDB 三层存储分区

```typescript
// IndexedDB 数据库版本 = 2
// Object Store 分层设计

// Layer 1: 原始录制数据（V1.0 保留）
const RAW_STORES = {
  projects:       { keyPath: 'id' },
  sessions:       { keyPath: 'id', indexes: ['projectId'] },
  steps:          { keyPath: 'id', indexes: ['sessionId', 'stepIndex'] },
  screenshots:    { keyPath: 'id', indexes: ['stepId'] },
  masking_rules:  { keyPath: 'id', indexes: ['profileId'] },
  documents:      { keyPath: 'id', indexes: ['sessionId'] },
};

// Layer 2: 知识图谱（V2.0 新增）
const KNOWLEDGE_STORES = {
  ontology_nodes: { keyPath: 'id', indexes: ['type', 'projectId'] },
  ontology_edges: { keyPath: 'id', indexes: ['fromNodeId', 'toNodeId', 'type'] },
  ui_concepts:    { keyPath: 'id', indexes: ['pageId', 'domFingerprint'] },
  biz_workflows:  { keyPath: 'id', indexes: ['projectId'] },
};

// Layer 3: 系统复刻（V2.0 新增）
const SYSTEM_STORES = {
  system_schemas:          { keyPath: 'id', indexes: ['projectId'] },
  system_modules:          { keyPath: 'id', indexes: ['schemaId'] },
  data_entities:           { keyPath: 'id', indexes: ['schemaId'] },
  interaction_patterns:    { keyPath: 'id', indexes: ['urlPattern'] },
};
```

---

## 八、接口文档（V2.0 新增部分）

### 8.1 新增消息类型

```typescript
type MessageType =
  // ... V1.0 原有消息类型 ...

  // V2.0 新增：知识库操作
  | 'KNOWLEDGE_QUERY_SIMILAR_STEPS'     // 查询相似步骤
  | 'KNOWLEDGE_INFER_WORKFLOW'          // 推断业务流程
  | 'KNOWLEDGE_EXPORT_SCHEMA'           // 导出系统 Schema
  | 'KNOWLEDGE_GET_MODULE_INFO'         // 获取模块知识

  // V2.0 新增：系统推断
  | 'SYSTEM_INFER_SCHEMA'               // 触发系统架构推断
  | 'SYSTEM_SCHEMA_UPDATE'              // Schema 更新通知
  | 'SYSTEM_EXPORT_OPENAPI'             // 导出为 OpenAPI Spec
  | 'SYSTEM_EXPORT_TEST_SKELETON'       // 导出测试骨架

  // V2.0 新增：VLM 路由
  | 'VLM_PROVIDER_STATUS_REQUEST'       // 查询 VLM 可用状态
  | 'VLM_PROVIDER_STATUS_RESPONSE';     // 返回各 VLM 状态
```

### 8.2 VLM 状态查询接口

```
GET (Message) VLM_PROVIDER_STATUS_REQUEST
Response: {
  providers: Array<{
    id: string;
    name: string;
    status: 'available' | 'quota_exceeded' | 'no_api_key' | 'unreachable';
    remainingQuota?: number;
    isCurrentlySelected: boolean;
  }>;
}
```

### 8.3 知识库查询接口

```
POST (Message) KNOWLEDGE_QUERY_SIMILAR_STEPS
Payload: {
  domFingerprint: string;
  action: ActionType;
  limit?: number;   // 默认 5
}
Response: {
  steps: Array<RecordingStep & { similarity: number }>;
}

POST (Message) SYSTEM_INFER_SCHEMA
Payload: { projectId: string; }
Response: {
  schemaId: string;
  modulesCount: number;
  entitiesCount: number;
  workflowsCount: number;
  confidence: number;
}
```

---

## 九、关键业务时序图（V2.0 新增）

### 9.1 步骤到知识图谱的提炼时序

```
┌──────┐  ┌────────────────┐  ┌─────────────────────┐  ┌───────────┐  ┌──────────┐
│事件触发│  │ Content Script │  │   Service Worker    │  │ Ontology  │  │IndexedDB │
│(步骤) │  │(STEP_CAPTURED) │  │  Knowledge Manager  │  │  Engine   │  │(知识图谱) │
└──┬───┘  └──────┬─────────┘  └──────────┬──────────┘  └─────┬─────┘  └────┬─────┘
   │              │                       │                   │              │
   │ 步骤完成     │                       │                   │              │
   │─────────────>│                       │                   │              │
   │              │ STEP_CAPTURED         │                   │              │
   │              │──────────────────────>│                   │              │
   │              │                       │ extractFromStep() │              │
   │              │                       │──────────────────>│              │
   │              │                       │                   │ upsertUIPage │
   │              │                       │                   │ (URL 去重)    │
   │              │                       │                   │─────────────>│
   │              │                       │                   │ upsertUIComp │
   │              │                       │                   │ (指纹去重)    │
   │              │                       │                   │─────────────>│
   │              │                       │                   │ buildEdges() │
   │              │                       │                   │─────────────>│
   │              │                       │ <── nodeIds ───── │              │
   │              │                       │                   │              │
   │              │                       │  更新 step.ontologyNodeIds       │
   │              │                       │──────────────────────────────────>│
   │              │ <── {stepId, nodeIds} ─│                  │              │
```

### 9.2 会话结束后业务流程提炼时序

```
┌──────┐  ┌──────────┐  ┌─────────────────────┐  ┌──────────┐  ┌───────────┐
│  PM  │  │  Popup   │  │   Service Worker    │  │ LLM API  │  │IndexedDB  │
│(User)│  │ (React)  │  │(Workflow Extractor) │  │(文本模型) │  │(知识图谱)  │
└──┬───┘  └────┬─────┘  └──────────┬──────────┘  └────┬─────┘  └─────┬─────┘
   │            │                  │                  │               │
   │ 点击"停止录制"                 │                  │               │
   │───────────>│                  │                  │               │
   │            │ SESSION_STOP     │                  │               │
   │            │─────────────────>│                  │               │
   │            │                  │ loadAllSteps()   │               │
   │            │                  │──────────────────────────────────>│
   │            │                  │ <── steps[] ──── │               │
   │            │                  │                  │               │
   │            │                  │ 构造 Workflow 提炼 Prompt          │
   │            │                  │ (纯文本，无图像，省 Token)          │
   │            │                  │─────────────────>│               │
   │            │                  │ <── 流程名/任务划分 ─│             │
   │            │                  │                  │               │
   │            │                  │ buildBizWorkflow()               │
   │            │                  │ 关联 UIPage 节点  │               │
   │            │                  │ 建立 BizTask → UIComponent 边    │
   │            │                  │──────────────────────────────────>│
   │            │ 知识提炼完成通知  │                  │               │
   │            │<─────────────────│                  │               │
   │ 显示"知识已更新" (N 个节点)    │                  │               │
   │<───────────│                  │                  │               │
```

---

## 十、V2.0 技术选型更新

| 类别 | V1.0 方案 | V2.0 方案 | 变更原因 |
|---|---|---|---|
| **首选 VLM** | GPT-4o-mini（付费） | Gemini 2.0 Flash / GLM-4V-Flash（免费） | 降低成本，保障政务场景预算 |
| **本地 VLM** | Ollama（可选） | Ollama qwen2.5-vl（推荐首选）| 零成本、零数据出境 |
| **文本 LLM（知识提炼）** | 同 VLM | Gemini 2.0 Flash / 智谱 glm-4-flash（免费） | 知识提炼不需要视觉，省 Token |
| **知识图谱存储** | 无 | IndexedDB（分层扩展）| 轻量，无需服务端 |
| **图算法库** | 无 | graphology（轻量图分析库） | 支持知识图谱遍历和模式识别 |
| **本地 VLM 运行** | 无 | Ollama（用户提前安装） | 可选依赖，无需打包进扩展 |
| **Extension 框架** | Chrome MV3 + React | 同左 | 无变化 |
| **构建工具** | Vite + CRXJS | 同左 | 无变化 |
| **导出引擎** | docx.js + jsPDF | 同左 + 新增 OpenAPI Schema 导出 | 支持系统复刻导出 |

---

## 十一、安全设计（V2.0 补充）

在 V1.0 五条原则基础上，新增：

6. **知识图谱脱敏存储**：`OntologyNode.label` 和 `UIComponentConcept.labelText` 均存储脱敏后文本，原始政务敏感词不进入知识图谱。
7. **LLM 调用降级保护**：知识提炼流程调用 LLM 失败时，优先进行规则化提炼（无 AI），确保知识入库不依赖外部服务。
8. **免费 API Key 隔离**：Gemini/智谱等免费 API Key 与付费 Key 分开存储，前者支持导出（用于分享配置），后者强制加密。

---

## 十二、目录结构（V2.0 更新）

```
g-pilot/
├── manifest.json
├── public/icons/
└── src/
    ├── background/
    │   ├── index.ts                    # 消息路由入口
    │   ├── session-manager.ts
    │   ├── ai/
    │   │   ├── orchestrator.ts         # AI 总调度
    │   │   ├── vlm-router.ts           # 免费优先 VLM 路由  ← V2.0 新增
    │   │   └── adapters/
    │   │       ├── gemini.adapter.ts   # ← V2.0 新增
    │   │       ├── zhipu.adapter.ts    # ← V2.0 新增
    │   │       ├── ollama.adapter.ts   # ← V2.0 新增
    │   │       ├── openrouter.adapter.ts # ← V2.0 新增
    │   │       └── openai.adapter.ts
    │   ├── knowledge/                  # ← V2.0 全新模块
    │   │   ├── knowledge-manager.ts    # 知识库管理器
    │   │   ├── ontology-engine.ts      # 本体推理引擎
    │   │   ├── workflow-extractor.ts   # 业务流程提炼
    │   │   └── system-inferrer.ts      # 系统 Schema 推断
    │   ├── replica/                    # ← V2.0 全新模块
    │   │   ├── business-replicator.ts  # 系统复刻器
    │   │   └── exporters/
    │   │       ├── openapi.exporter.ts
    │   │       └── playwright.exporter.ts
    │   ├── doc-generator.ts
    │   ├── export-engine.ts
    │   ├── storage-manager.ts          # 升级为三层存储
    │   └── config-manager.ts
    ├── content/
    │   ├── index.ts
    │   ├── semantic-parser.ts          # 新增 DOM 指纹生成
    │   ├── masking-engine.ts
    │   ├── screenshot-capture.ts
    │   └── floating-console/
    ├── popup/
    │   ├── index.tsx
    │   └── pages/
    │       ├── ProjectList.tsx
    │       ├── RecordingControl.tsx
    │       ├── MaskingConfig.tsx
    │       ├── KnowledgeExplorer.tsx   # ← V2.0 新增：知识图谱浏览
    │       ├── SystemOntologyView.tsx  # ← V2.0 新增：系统架构视图
    │       └── DocPreview.tsx
    └── shared/
        ├── types/
        │   ├── recording.types.ts      # 原 V1.0 类型
        │   ├── ontology.types.ts       # ← V2.0 新增
        │   └── system-replica.types.ts # ← V2.0 新增
        ├── constants.ts
        ├── message-bus.ts
        └── utils/
            ├── crypto.ts
            ├── xpath.ts
            ├── dom-fingerprint.ts      # ← V2.0 新增：DOM 指纹算法
            └── graph-utils.ts          # ← V2.0 新增：图遍历工具
```

---

## 十三、路线图与阶段规划

| 版本 | 核心特性 | 依赖知识库能力 |
|---|---|---|
| **V1.0（基础）** | 录制 + 脱敏 + AI 文档生成 | 无 |
| **V2.0（本版）** | 免费 VLM 优先 + 知识图谱入库 + 系统 Schema 推断 | 知识图谱构建 |
| **V3.0（扩展）** | 基于知识库的自动 E2E 测试用例生成 | 知识图谱 + 业务流程 |
| **V4.0（智能）** | AI 培训助手：基于知识库回答"如何操作XX功能" | 全量知识图谱 + RAG |
| **V5.0（RPA）** | 基于业务流程自动生成 RPA 自动化脚本 | 系统 Schema + Workflow |

---

*文档结束。V2.0 在 V1.0 基础上重点升级了 AI 成本控制和数据价值复用两个维度，为 G-Pilot 从"文档工具"演进为"政务知识平台"奠定架构基础。*
